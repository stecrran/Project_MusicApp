<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Standard output log configuration -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress HTTP wire-level logging -->
    <logger name="org.apache.hc.client5.http.wire" level="OFF"/>
    <logger name="org.apache.hc.core5.http.impl.io" level="OFF"/>
    <logger name="org.apache.hc.client5.http.impl.classic" level="OFF"/>
    <logger name="org.apache.hc.core5.http" level="OFF"/>

    <!-- Optionally, set your root logger level -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
