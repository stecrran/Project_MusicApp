# Dockerfile.selenium-test

FROM maven:3.9-eclipse-temurin-17 AS builder
WORKDIR /tests
COPY . .
RUN mvn clean package -DskipTests

FROM maven:3.9-eclipse-temurin-17
WORKDIR /tests
COPY --from=builder /tests /tests

# Default to idle mode
CMD ["tail", "-f", "/dev/null"]
